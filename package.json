
{
  "name": "snow-nft",
  "version": "0.0.1",
  "description": "A project for Solidity Boot Camp",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.ts"
  },
  "author": "Caven",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "dotenv": "^16.3.1",
    "hardhat": "^2.19.1"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  },
  "pnpm": {
    "version": "8.11.0"
  }
}
